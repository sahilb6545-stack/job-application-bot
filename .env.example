# ============================================================
# JOB APPLICATION BOT - CONFIGURATION
# ============================================================
# Copy this file to .env and fill in your values.
# For GitHub Actions, add each as a Repository Secret.
# ============================================================

# --- LinkedIn Credentials ---
LINKEDIN_EMAIL=your_linkedin_email@example.com
LINKEDIN_PASSWORD=your_linkedin_password

# --- Indeed Credentials ---
INDEED_EMAIL=your_indeed_email@example.com
INDEED_PASSWORD=your_indeed_password

# --- Email Notifications (Gmail) ---
# Use a Gmail App Password (not your regular password).
# Enable 2FA on your Google account, then generate an App Password:
# https://myaccount.google.com/apppasswords
SMTP_EMAIL=your_gmail@gmail.com
SMTP_PASSWORD=your_gmail_app_password
NOTIFY_EMAIL=your_email@example.com

# --- Bot Settings ---
# Maximum applications per cycle (to avoid rate limits)
MAX_APPLICATIONS_PER_RUN=50

# Time between application cycles in seconds (600 = 10 minutes)
CYCLE_INTERVAL_SECONDS=600

# Delay between actions in seconds (randomized ±30%)
ACTION_DELAY_SECONDS=3

# Location preference for job search
PREFERRED_LOCATION=Toronto, ON, Canada

# Remote preference: true/false
PREFER_REMOTE=true
